# About
This might someday be a small benchmark suite for various inter-process communication mechanisms, which is currently in the prototyping phase.
# Usage
```
> cargo build --release
> cargo run --release --bin ipc_benchmark
```
